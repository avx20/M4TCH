// Apply plugins for all modules
plugins {
  id 'base'
}

// Global repositories for all modules
allprojects {
  repositories {
    mavenCentral()
  }
}

// Shared dependencies (Optional, can be added in specific modules)
subprojects {
  apply plugin: 'java'
}
